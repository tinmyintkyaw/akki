version: "3"

services:
  web:
    container_name: web
    build:
      context: .
      dockerfile: next.prod.Dockerfile
    restart: unless-stopped
    ports:
      - "3000:3000"
    networks:
      - app-network

networks:
  app-network:
    external: true
