type typesenseDocument = {
  id: string;
  pageName: string;
  textContent: string;
  createdAt: number;
  modifiedAt: number;
  isFavourite: boolean;
  userId: string;
};

export default typesenseDocument;
